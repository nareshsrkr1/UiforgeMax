{
  "name": "platform",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "portal:dev": "npm --prefix apps/customer-portal run dev",
    "api:dev": "npm --prefix apps/customer-api run dev"
  }
}
